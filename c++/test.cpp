#include <bits/stdc++.h>

using namespace std;

int main() {
	int n;
	cin >> n;
	vector<int> a(n);
	for (int i = 1; i <= n; i++) {
		cin >> a[i];
	}
	vector<bool> visited(n, false);
	for (int i = 0; i < n; i++) {
		if (visited[i] == false) {
			for (int j = i + 1; j < n; j++) {
				if (a[i] 
			}
		}
	}

	return 0;
}
